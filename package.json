{
  "name": "flashgateway",
  "private": true,
  "scripts": {
    "start": "docker compose up --build",
    "start:api": "docker compose up --build api",
    "start:web": "docker compose up --build web",
    "start:db": "docker compose up --build db",
    "down": "docker compose down",
    "logs": "docker compose logs --follow"
  }
}
